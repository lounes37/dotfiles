matches:
  - trigger: 'single_list'
    replace: "{{output}}"
    vars:
      - name: output
        type: script
        params:
          args:
            - pwsh
            - -File
            - "%CONFIG%/scripts/toSingleQuoteArray.ps1"